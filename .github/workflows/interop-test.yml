name: Interoperability Testing

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'config/**'
      - 'core/**'
      - 'internal/**'
      - 'p2p/**'
      - 'test-plans/**'
      - '.github/workflows/interop-test.yml'
  push:
    branches:
      - "master"
    paths:
      - 'config/**'
      - 'core/**'
      - 'internal/**'
      - 'p2p/**'
      - 'test-plans/**'

jobs:
  run-transport-interop:
    name: Run transport interoperability tests
    runs-on: ubuntu-latest
    steps:
      - name: Free Disk Space (Ubuntu)
        if: vars['INTEROP_TEST_RUNNER_UBUNTU'] == ''
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1
        with:
          tool-cache: true
      - uses: actions/checkout@v4

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          yq --version

      - name: Checkout libp2p/test-plans
        uses: actions/checkout@v4
        with:
          repository: libp2p/test-plans
          path: test-plans-harness

      - name: Build go-libp2p-head image (transport-go-libp2p-head)
        run: docker build -t transport-go-libp2p-head -f test-plans/PingDockerfile .

      - name: Inject go-libp2p-head into test-plans transport/images.yaml
        working-directory: test-plans-harness/transport
        env:
          GO_LIBP2P_DIR: ${{ github.workspace }}
        run: |
          set -euo pipefail

          # Remove any existing entry (for re-runs), then append our local build.
          yq eval -i \
            '.implementations = ((.implementations // []) | map(select(.id != "go-libp2p-head"))) + [{
              "id": "go-libp2p-head",
              "source": {
                "type": "local",
                "path": strenv(GO_LIBP2P_DIR),
                "dockerfile": "test-plans/PingDockerfile"
              },
              "transports": ["tcp", "ws", "wss", "quic-v1", "webtransport", "webrtc-direct"],
              "secureChannels": ["tls", "noise"],
              "muxers": ["yamux"]
            }]' \
            images.yaml

      - name: Run transport interop (bash harness)
        working-directory: test-plans-harness/transport
        env:
          # Keep cache writable on github-hosted runners
          CACHE_DIR: ${{ github.workspace }}/.cache
        run: |
          ./run.sh \
            --test-select "go-libp2p-head" \
            --cache-dir "${CACHE_DIR}" \
            --workers 2 \
            -y
