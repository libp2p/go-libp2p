version: "2"

run:
  timeout: 5m

linters:
  enable:
    - revive
    - unused
    - prealloc

  disable:
    - staticcheck
    - govet

  settings:
    revive:
      severity: warning
      rules:
        - name: unused-parameter
          severity: warning

severity:
    default: warning

